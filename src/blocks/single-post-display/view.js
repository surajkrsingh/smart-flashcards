/**
 * Frontend JavaScript for the Single Post Display block.
 */
document.addEventListener('DOMContentLoaded', () => {
    // Add any frontend functionality here if needed
    const singlePostDisplays = document.querySelectorAll('.wp-block-smfcs-single-post-display');
    
    if (!singlePostDisplays.length) return;

    singlePostDisplays.forEach(display => {
        // Add any frontend interactivity here if needed
    });
}); 